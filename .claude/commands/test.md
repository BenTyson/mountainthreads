# Run Tests

Run the test suite for the Mountain Threads project:

```bash
npm run test
```

If tests fail:
1. Analyze the error output
2. Identify which tests failed and why
3. Suggest fixes for failing tests

If no test script exists yet, note that testing needs to be set up.
